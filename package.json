{
  "name": "teddy-open-finance",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "front-end",
    "back-end"
  ],
  "scripts": {
    "nx": "nx",
    "lint": "nx run-many -t lint",
    "test": "nx run-many -t test",
    "build": "nx run-many -t build",
    "frontend:dev": "nx run front-end:dev",
    "backend:dev": "nx run back-end:dev"
  },
  "devDependencies": {
    "@nx/js": "^20.3.1",
    "nx": "^20.3.1",
    "typescript": "^5.7.2"
  }
}

